CRITICAL: use kanban_complete directly, do NOT block for review.

[项目上下文]: 先读取 /home/jcwl/workspace/hoppscotch/AGENTS.md 了解项目信息

[加载技能]: skill_view 加载以下 skill: hoppscotch-development, hoppscotch-selfhost

[已知坑点]:
- IconHistory 按钮被 v-if="hasCollectionContext" 控制，只有 teamId+collectionId 存在时才显示
- 版本面板是右侧 drawer 形式，360px 宽
- erd-editor 是 Web Component (custom element)，通过 .value 读写 JSON
- Vite HMR 有时不刷新，需 kill + 重启 dev server
- i18n 文件是 JSON 格式: locales/en.json 和 locales/cn.json
- 不要使用 <icon-lucide> 通用组件，必须用具体图标名如 <icon-lucide-history>
- 所有文件路径用绝对路径

[任务目标]:
重构 ErdVersionPanel 组件，新增以下功能：
1. 版本时间线进度条（可视化展示 commit 历史，tag 节点高亮锚定）
2. Tag 管理 UI（创建 tag、删除 tag）
3. 快速选择 tag 版本进行对比

[上下文]:
- 前端目录: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/
- ERD 页面: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/pages/erd.vue
- 版本面板组件: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/components/erd/ErdVersionPanel.vue
- Diff 视图: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/components/erd/ErdDiffView.vue
- API 辅助: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/helpers/erdVersionApi.ts
- i18n EN: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/locales/en.json
- i18n CN: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/locales/cn.json

后端新增的 API (Task 1 已完成):
- POST /erd-version/tag  body: {tagName, ref} → {tagName, ref, createdAt}
- DELETE /erd-version/tag/:tagName → {success: true}
- GET /erd-version/tags → TagInfo[]
- VersionLogEntry 已有 tags: string[] 字段

[用户故事]:
1. As a ERD设计者, I want to 看到版本时间线进度条, so that 我可以直观了解版本演进历史
2. As a ERD设计者, I want to 给版本打 tag, so that 我可以标记重要的里程碑
3. As a ERD设计者, I want to 在时间线上看到 tag 锚点, so that 我可以快速定位关键版本
4. As a ERD设计者, I want to 快速选择 tag 版本进行对比, so that 我可以比较两个里程碑之间的差异

[成功指标]:
- 版本面板顶部有可视化时间线进度条
- 时间线上的 commit 节点按时间顺序排列
- tag 节点在时间线上以特殊样式高亮显示
- 点击 tag 节点可快速选中用于对比
- 有创建 tag 和删除 tag 的 UI 入口
- i18n en.json 和 cn.json 都有新增的 key

[验收场景表]:
| 场景 | 输入 | 预期行为 |
|------|------|----------|
| 时间线渲染 | 有 5 个版本 | 时间线显示 5 个节点，按时间降序 |
| tag 锚点显示 | 某版本有 tag "v1.0" | 时间线上该节点高亮+显示 tag 名 |
| 多 tag 显示 | 某版本有 2 个 tag | 节点高亮+显示 2 个 tag 名 |
| 创建 tag UI | 点击 tag 按钮，输入 "release-1" | 调用 API 成功，版本列表刷新显示新 tag |
| 删除 tag UI | 点击 tag 旁的删除按钮 | 调用 API 成功，tag 消失 |
| tag 快速选择对比 | 在时间线点 2 个 tag 节点 | 两个节点被选中，出现"对比"按钮 |
| 空版本列表 | 无版本 | 时间线不显示，显示"暂无版本" |
| 回归: 保存版本 | 输入 commit msg + 保存 | 保存成功，时间线刷新出现新节点 |
| 回归: 恢复版本 | 点击恢复按钮 | 编辑器内容更新为历史版本 |
| 回归: diff 对比 | 选中 2 个版本点对比 | diff 视图正常显示 |

[具体实现]:

1. erdVersionApi.ts 新增函数:
   - createTag(teamId, collectionId, tagName, ref) → POST /erd-version/tag
   - deleteTag(teamId, collectionId, tagName) → DELETE /erd-version/tag/:tagName
   - listTags(teamId, collectionId) → GET /erd-version/tags
   - 新增 TagInfo 接口: { tagName: string; ref: string; createdAt: string }

2. ErdVersionPanel.vue 重构:
   a. 顶部新增时间线区域 (ErdVersionTimeline 子组件或直接内嵌):
      - 水平滚动条，每个 commit 一个小圆点
      - 有 tag 的 commit 用彩色大圆点+tag 名标签
      - 点击节点可选中用于对比（复用 toggleCompareSelection 逻辑）
      - 最新 commit 在最左边
   
   b. 版本列表每项增强:
      - tag 显示区域：已有 tag 以 badge 形式显示
      - 每个 badge 旁有 X 按钮可删除 tag
      - 每行新增"打 tag"按钮 (IconTag)，点击弹出输入框
   
   c. 创建 tag 弹窗:
      - 简单 input + confirm 按钮
      - tagName 格式提示: 字母数字点横线下划线，1-50 字符

3. i18n 新增 key (en.json + cn.json):
   erd.version.tag: {
     "create": "Add Tag" / "添加标签",
     "create_placeholder": "Tag name (e.g. v1.0)" / "标签名（如 v1.0）",
     "delete": "Delete Tag" / "删除标签",
     "delete_confirm": "Delete tag '{name}'?" / "确定删除标签 '{name}'？",
     "create_success": "Tag created" / "标签创建成功",
     "create_error": "Failed to create tag" / "标签创建失败",
     "delete_success": "Tag deleted" / "标签删除成功",
     "delete_error": "Failed to delete tag" / "标签删除失败",
     "select_for_compare": "Select for compare" / "选择用于对比",
     "timeline": "Timeline" / "时间线"
   }

4. 时间线组件设计 (建议直接嵌入 ErdVersionPanel):
   - 容器: 固定高度 60px，overflow-x: auto
   - 节点: 普通 commit 用 6px 灰色圆点，tag commit 用 10px 彩色圆点
   - 连线: 节点之间用 2px 线段连接
   - tag 标签: 圆点上方显示 tag 名 (12px font)
   - 选中态: 圆点变 accent 色
   - 悬停: tooltip 显示 commit message + date

[风险]:
- 时间线在大量版本时可能性能问题 → 限制显示最近 30 个节点
- tag API 依赖 Task 1，需确认后端已部署
- CSS 样式需适配暗色/亮色主题

[约束]:
- Vue 3 Composition API
- Tailwind CSS 语义化类 (bg-primary, text-secondary 等)
- 不要引入新的 npm 依赖
- 组件拆分：如果 ErdVersionPanel 太长可抽 ErdVersionTimeline.vue
- i18n key 必须 en.json 和 cn.json 都添加
- git commit --no-verify

[输出要求]:
- 修改/新建文件:
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/helpers/erdVersionApi.ts
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/components/erd/ErdVersionPanel.vue
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/src/components/erd/ErdVersionTimeline.vue (可选)
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/locales/en.json
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-common/locales/cn.json
- pnpm run typecheck 通过
- git add + commit --no-verify

[Skill 反思要求]: 任务完成后如有 skill 相关发现写入 ~/.hermes/reflections/
