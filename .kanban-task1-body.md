CRITICAL: use kanban_complete directly, do NOT block for review.

[项目上下文]: 先读取 /home/jcwl/workspace/hoppscotch/AGENTS.md 了解项目信息

[加载技能]: skill_view 加载以下 skill: hoppscotch-development, hoppscotch-selfhost

[已知坑点]:
- simple-git 的 tag 方法: git.raw(['tag', tagName, ref]) 创建 lightweight tag
- 删除 tag: git.raw(['tag', '-d', tagName])
- 列出 tags: git.raw(['tag', '-l', '--format=%(refname:short) %(objectname:short) %(creatordate:iso)'])
- controller 中 validateRef 当前只接受 hex hash 和 HEAD~N 格式，tag 名需要同步支持
- 所有文件路径用绝对路径
- commit 用 --no-verify，push 用 background=true

[任务目标]:
为 ERD 版本管理系统添加 Tag（标签）CRUD API，使用户可以对特定版本打标签。

[上下文]:
- 后端目录: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/erd-version/
- DTO 目录: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/erd-version/dto/
- errors.ts: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/errors.ts
- service 使用 simple-git 管理 per-collection git 仓库
- 已有 VersionLogEntry.tags 字段，getVersionLog 已经解析 git refs 中的 tag
- 现有 DTO 参考: /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/erd-version/dto/commit-version.dto.ts

[用户故事]:
As a ERD设计者, I want to 给特定版本打tag标签, so that 我可以标记重要里程碑版本

[成功指标]:
- POST /erd-version/tag 创建 tag 成功 (HTTP 201)
- DELETE /erd-version/tag/:tagName 删除 tag 成功 (HTTP 200)
- GET /erd-version/tags 返回 tag 列表 (HTTP 200)
- validateRef() 支持 tag 名作为 ref（diff/restore 可用 tag）
- pnpm run build 通过

[验收场景表]:
| 场景 | 输入 | 预期行为 |
|------|------|----------|
| 正常创建 tag | tagName='v1.0', ref=有效commitHash | HTTP 201, body: {tagName, ref, createdAt} |
| 重复 tag 名 | tagName 已存在 | HTTP 409, message 含 'already exists' |
| 无效 ref | ref=不存在的hash | HTTP 400, message 含 'ref not found' |
| 正常删除 tag | tagName 存在 | HTTP 200, {success: true} |
| 删除不存在 tag | tagName 不存在 | HTTP 404, message 含 'not found' |
| 列出 tags | 有 3 个 tag | HTTP 200, 数组长度=3 |
| tag 名注入攻击 | tagName=';rm -rf /' | HTTP 400, 验证失败 |
| 用 tag 调 diff | from=tag名 | diff 正常返回 |
| 用 tag 调 restore | ref=tag名 | 返回 erdJson |
| 回归 commit | 正常 commit | 已有功能不受影响 |

[具体实现]:

1. errors.ts 新增:
   export const ERD_VERSION_TAG_CREATE_FAILED = 'erd_version/tag_create_failed' as const;
   export const ERD_VERSION_TAG_DELETE_FAILED = 'erd_version/tag_delete_failed' as const;
   export const ERD_VERSION_TAG_EXISTS = 'erd_version/tag_exists' as const;

2. dto/create-tag.dto.ts 新文件:
   class CreateTagDto {
     @IsString() @Matches(/^[a-zA-Z0-9._-]{1,50}$/) tagName: string;
     @IsString() ref: string;
   }

3. erd-version.service.ts 新增方法:
   - createTag(teamId, collectionId, tagName, ref): git.raw(['tag', tagName, ref])
   - deleteTag(teamId, collectionId, tagName): git.raw(['tag', '-d', tagName])
   - listTags(teamId, collectionId): git.raw(['tag', '-l', '--format=...']) 解析输出
   - 私有 validateTagName(name): /^[a-zA-Z0-9._-]{1,50}$/

4. erd-version.controller.ts:
   - 新增 POST 'tag' (body: CreateTagDto)
   - 新增 DELETE 'tag/:tagName'
   - 新增 GET 'tags'
   - 更新 validateRef() 支持 tag 名 (先用 git rev-parse --verify 验证，或简单允许 [a-zA-Z0-9._-] 格式)

[风险]:
- validateRef 更新可能导致注入风险，必须严格验证 tag 名格式
- tag 名和 commit hash 可能冲突，注意顺序优先级

[约束]:
- NestJS + class-validator
- JWT 认证 + team 成员验证
- per-collection mutex 保护
- 所有路径用绝对路径

[输出要求]:
- 修改/新建以下文件:
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/errors.ts
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/erd-version/dto/create-tag.dto.ts
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/erd-version/erd-version.service.ts
  - /home/jcwl/workspace/hoppscotch/packages/hoppscotch-backend/src/erd-version/erd-version.controller.ts
- 运行 pnpm run build 验证编译通过
- git add + commit --no-verify

[Skill 反思要求]: 任务完成后如有 skill 相关发现写入 ~/.hermes/reflections/
