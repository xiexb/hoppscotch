import { Injectable } from '@nestjs/common';

/**
 * ErdVersionService — manages ERD version history via git.
 *
 * This is a placeholder. Full implementation in a subsequent task.
 */
@Injectable()
export class ErdVersionService {
  // Methods to be implemented:
  // - commitVersion(erdJson, message?)
  // - getVersionLog()
  // - getVersion(ref)
  // - getDiff(from?, to?)
  // - restoreVersion(ref)
  // - deleteVersion(ref)
  // - exportArchive(ref?)
}
