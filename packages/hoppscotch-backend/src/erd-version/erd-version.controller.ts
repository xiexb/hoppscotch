import { Controller } from '@nestjs/common';

/**
 * ErdVersionController — REST endpoints for ERD version management.
 *
 * This is a placeholder. Full implementation in a subsequent task.
 *
 * Planned endpoints:
 *   POST   /api/erd/version/commit
 *   GET    /api/erd/version/log
 *   GET    /api/erd/version/:ref
 *   GET    /api/erd/version/diff
 *   POST   /api/erd/version/restore/:ref
 *   DELETE /api/erd/version/:ref
 *   POST   /api/erd/version/export
 */
@Controller({ path: 'erd/version', version: '1' })
export class ErdVersionController {
  // Endpoints to be implemented in a subsequent task
}
